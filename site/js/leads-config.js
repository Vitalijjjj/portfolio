/*
 * Куди надсилати заявки з форми.
 *
 * Варіант A (рекомендований, токен прихований на сервері):
 *   proxyUrl — URL Supabase Edge Function, яка сама викликає Telegram.
 *   Токен і chat_id зберігаються як секрети функції, у браузер не потрапляють.
 *   Інструкція: supabase/functions/README.md
 *
 * Варіант B (працює одразу, без деплою):
 *   botToken + chatId нижче. УВАГА: ці значення видно всім у коді сторінки —
 *   сторонні можуть надсилати повідомлення від імені бота у цей чат.
 *   Використовуйте, поки не задеплоїте Edge Function.
 *
 * Якщо proxyUrl заповнений — використовується він, botToken ігнорується.
 */
window.LEADS_CONFIG = {
  proxyUrl: "",
  botToken: "***REMOVED***",
  chatId: "-5289373638",
  // Назва, яка підписує повідомлення (щоб було видно джерело заявки)
  siteName: "gural.digital"
};
